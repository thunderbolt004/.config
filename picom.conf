fading = true;
fade-in-step = 0.04;
fade-out-step = 0.04;

backend = "glx";
glx-no-stencil = true;
glx-no-rebind-pixmap = false;
vsync = true;

opacity-rule = [
    "90:class_g = 'Nemo'",
];

blur:
{
  method = "gaussian";
  size = 10;
  deviation = 5.0;
};

blur-background-exclude = [
    "window_type = 'dock'",
    # for maim screenshot
    "name ~= 'slop'",
];
